# 2026-03-31（週二）｜第 2 週

**預估時間：1 小時**

## 今日學習重點

- 錯誤訊息顯示在欄位**旁邊或下方**（不要只用 `alert`）
- 用 DOM 新增/修改一個放錯誤文字的元素，或用 `class` 切換顯示

## 建議學習方式

1. HTML 每個欄位外面包一層，預留 `<span class="error-msg"></span>`。
2. 驗證失敗時填入文字、成功時清空；可加紅色樣式 class。

## 作業

1. 兩個欄位：姓名、電話（可先不驗格式，只驗必填）。
2. 錯誤時在該欄位下方顯示紅字；正確時隱藏錯誤。
3. 嘗試：用一個函式 `showError(fieldId, message)` 統一處理。

## 完成標準（自檢）

- [ ] 使用者看得出「哪一欄錯了」
- [ ] 修正輸入後再次送出，錯誤能消失（至少手動再送一次時）
