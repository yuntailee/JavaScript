# 2026-04-10（週五）｜第 3 週

**預估時間：1 小時**

## 今日學習重點

- 把 **第 2 週註冊表單** 改寫成 **jQuery 版**
- 驗證邏輯可沿用，改成用 `$` 取 DOM

## 建議學習方式

1. 複製第 2 週專題到新資料夾 `week3-jquery-register`，避免覆蓋舊檔。
2. 逐段替換：`querySelector` → `$`、`addEventListener` → `.on`。

## 作業

1. 功能與第 2 週相同：四欄驗證 + 錯誤顯示 + 通過才允許「送出」。
2. 送出若仍用 `console.log` 即可；若已會 `$.post`，可選接假 API。
3. 在檔案頂部註解：原生與 jQuery 各一個優缺點（自己實際感受）。

## 完成標準（自檢）

- [ ] 行為與第 2 週等價
- [ ] 沒有混用同一個按鈕綁兩次 submit 導致跑兩次（注意重複綁定）
