# 2026-03-29（週日）｜第 1 週 · 週末專題

**預估時間：約 3 小時**（建議：50 分鐘寫作 + 10 分鐘休息，共 3 輪；最後 30 分鐘複習本週筆記）

## 今日學習重點

- 整合本週：變數、函式、DOM、事件
- 完成一個可展示的小頁面（給自己或履歷用）

## 建議學習方式

1. **第 1 輪（50 分鐘）**：只做出 HTML 結構 + 基本 CSS（可很醜，能看即可）。
2. **第 2 輪（50 分鐘）**：寫 JS：取 DOM、綁事件、顯示結果。
3. **第 3 輪（50 分鐘）**：修 bug、加一個小細節（例如按 Enter 也算送出、或清空按鈕）。
4. **最後 30 分鐘**：把本週不懂的名詞寫成 5 條「之後要查」的清單。

## 作業（週末小專題）

做 **一個 HTML 頁面**：

1. 兩個輸入框：例如「名字」「暱稱」或「名字」「一句話」。
2. 一個按鈕（或表單送出）。
3. 按下後在**頁面上**（不是只有 Console）顯示：**「你好，{名字}」**（名字用第一個輸入框的值）。
4. 檔名建議：`week1-greeting/index.html`、`main.js`、`style.css`（可選）。

## 完成標準（自檢）

- [ ] 不開 Console 也能完成互動
- [ ] 程式至少拆成 2 個函式（例如 `getInputValues`、`showGreeting`）或註解說明意圖
- [ ] 能用自己的話講一次：從按下按鈕到畫面更新，發生了什麼
