# 2026-04-02（週四）｜第 2 週

**預估時間：1 小時**

## 今日學習重點

- `input` 事件：輸入時即時反應
- `submit`：送出時再一次檢查
- 兩種時機的差異與取捨

## 建議學習方式

1. 同一個欄位：`input` 時更新提示（例如「還差 3 字」）；`submit` 時做最終審判。
2. 避免在 `input` 裡過度 `alert`，用畫面提示即可。

## 作業

1. 密碼欄：`input` 時若少於 6 字，顯示黃色提示；`submit` 時若仍不足則挡下並顯示錯誤。
2. 寫註解：為什麼 `submit` 不能省（即使有 `input` 驗證）。

## 完成標準（自檢）

- [ ] 兩種事件都有綁在同一表單上
- [ ] 能說出即時驗證與送出驗證各解決什麼問題
