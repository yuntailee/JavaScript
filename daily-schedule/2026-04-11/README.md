# 2026-04-12（週日）｜第 3 週 · 週末專題

**預估時間：約 3 小時**

## 今日學習重點

- 登入表單（帳密）+ jQuery 驗證
- 「假送出」：`$.post` 成功 callback 裡 `alert` 或顯示訊息
- 整理：**jQuery 與原生差異**筆記

## 建議學習方式

1. 帳密規則自訂（例如帳號 4～20 字、密碼至少 8 字）。
2. 最後 1 小時寫 `jquery-vs-vanilla.md`：列 5 點對照。

## 作業（週末小專題）

1. **登入頁**：帳號、密碼、送出鈕；錯誤訊息用 jQuery 操作 DOM。
2. 驗證通過後：`$.post` 到 JSONPlaceholder（或僅在 `success` 裡 `alert('登入成功（假）')`）。
3. 同一資料夾新增筆記檔 `notes-jquery.md`：本週最常用到的 5 個 API（例如 `$`、`on`、`val`、`serialize`、`post`）。

## 完成標準（自檢）

- [ ] 驗證失敗時不會發 AJAX
- [ ] 有一份自己能複習的 jQuery 對照筆記
