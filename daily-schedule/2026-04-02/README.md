# 2026-04-03（週五）｜第 2 週

**預估時間：1 小時**

## 今日學習重點

- 任一欄位錯誤就**不送出**
- 全對才執行「送出邏輯」：本日用 `console.log('送出成功', data)` 模擬

## 建議學習方式

1. 寫 `validateForm()` 回傳 `true/false` 或收集所有錯誤。
2. `submit` 裡先跑驗證，通過才 `console.log`。

## 作業

1. 整合本週欄位：姓名（必填）、手機（數字）、Email（regex）、密碼（至少 6 字）。
2. 錯誤時 `preventDefault`，並聚焦第一個錯誤欄位（選做：`focus()`）。
3. 全對時 `console.log` 一個物件，包含所有欄位值（敏感資料僅練習用、勿用真實密碼）。

## 完成標準（自檢）

- [ ] 故意填錯時絕對不會印出「送出成功」
- [ ] 通過時印出的物件結構清楚
