# 2026-03-30（週一）｜第 2 週

**預估時間：1 小時**

## 今日學習重點

- 必填檢查：空字串、`trim()` 去掉前後空白
- 「只有空白」也算不合法

## 建議學習方式

1. 在表單輸入框讀值後立刻 `.trim()`。
2. 用 `if (!value)` 或長度判斷，試試 `""` 和 `"   "` 的差異。

## 作業

1. 一個輸入框 + 送出；若 trim 後為空字串，`alert` 或畫面上顯示「請填寫」。
2. 測試三種輸入：正常文字、全空白、前面有空格的姓名（trim 後應可接受）。

## 完成標準（自檢）

- [ ] 全空白無法通過
- [ ] 能說出為什麼要先 `trim` 再檢查
